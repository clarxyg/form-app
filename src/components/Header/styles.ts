import styled from "styled-components";

export const Container = styled.div`
   background: var(--header); 
   padding: 28px;

   h1 {
     margin: 0;
     padding: 0;

     font-size: 2rem;
     color: #f1f2f3;
   }
`

